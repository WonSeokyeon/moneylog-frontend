"use client";

// 실제 퀵 입력 바·목록·필터는 Phase 9에서 채운다. 컨테이너 폭/패딩은 (main)/layout.tsx가 이미 적용한다.
export default function TransactionsPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">거래 내역</h1>
      <p className="mt-2 text-sm text-muted-foreground">Phase 9에서 채울 예정입니다.</p>
    </div>
  );
}
