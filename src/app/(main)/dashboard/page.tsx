"use client";

// 실제 요약·차트·예측·예산은 Phase 10에서 채운다. 컨테이너 폭/패딩은 (main)/layout.tsx가 이미 적용한다.
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">대시보드</h1>
      <p className="mt-2 text-sm text-muted-foreground">Phase 10에서 채울 예정입니다.</p>
    </div>
  );
}
